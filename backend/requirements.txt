# Regal Park Villas — Backend Dependencies
# Python 3.11+
# NOTE: For Vercel serverless, use the root requirements.txt (slim version)

# ── Web framework ────────────────────────────────────────
fastapi>=0.110.0,<1.0
uvicorn>=0.25.0,<1.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# ── Database ─────────────────────────────────────────────
motor>=3.3.0,<4.0
pymongo>=4.5.0,<5.0
dnspython>=2.4.0

# ── Auth ─────────────────────────────────────────────────
bcrypt>=4.0.0
PyJWT>=2.8.0

# ── Security / Rate limiting ────────────────────────────
slowapi>=0.1.9
limits>=3.0.0

# ── HTTP / Validation ───────────────────────────────────
httpx>=0.25.0
email-validator>=2.0.0
pydantic>=2.5.0
